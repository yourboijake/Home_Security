#!/bin/bash


#should check for new messages in SMTP inbox
#if a message satisfies certain timestamp conditions, says "START", and the main python script isn't currently running, then start the python script
